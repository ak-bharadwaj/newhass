/**
 * mockData.ts has been intentionally removed.
 * Do not import from this module. Use real API calls from '@/lib/api'.
 * If you see this file imported anywhere, replace that code with real data fetching
 * and display honest empty states when backend data is unavailable.
 */

export const MOCK_DATA_REMOVED = true

export function __doNotUseMockData(): never {
  throw new Error('mockData.ts has been removed. Use real APIs from lib/api.ts.')
}
